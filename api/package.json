{
  "name": "api",
  "version": "0.0.1",
  "description": "API for full stack seed",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --import=tsx --watch --env-file=.env src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "Mihai Pistol",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
